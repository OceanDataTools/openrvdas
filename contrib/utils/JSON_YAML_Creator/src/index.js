import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Everything starts here. Calls the App component
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
