# From http:#www.gpsinformation.org/dale/nmea.htm
#
#
{
    # $GPGLL,3934.8390,S,03727.7066,W,000004.125,A*30
    # $GPGLL,lat,S,lon,W,fix_time,A*csum
    "$GPGLL": {
        #"format": "%f,%s,%f,%s,%f,%s*%s",
        "fields": [
            ["Latitude", "float"],
            ["NorS", "str"],
            ["Longitude", "float"],
            ["EorW", "str"],
            ["FixTime", "float"],
            ["Mode", "str"]
        ]
    }
}
