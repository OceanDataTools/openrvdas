{
    "Seapath330": {
        "messages": {
            "$INGGA": "$GPGGA",  # Reuse the $GP... definitions
            "$INHDT": "$GPHDT",  # Is there actually any difference?
            "$INVTG": "$GPVTG",
            "$INRMC": "$GPRMC",
            "$INZDA": "$GPZDA",
            "$PSXN" : "$PSXN"
        }
    }
}
