# A derived "alert" message, which is basically a single string.  We
# want whatever string we're given to be parsed into a single string
# for a single message field, so we use an improbable delimiter.
{
    "Alert": {
        "field_delimiter": "THIS_IS_NOT_A_PROBABLE_DELIMITER",
        "fields": [
            ["Message", "str"]
        ]
    }
}
