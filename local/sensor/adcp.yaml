{
    # data_id
    "adcp": {
        "name": "adcp",
        "model": "ADCP",    # references a definition in sensor_model/
        "fields": {               # references definitions in message/
        "EastVelocity": "ADCPEastVelocity",
        "NorthVelocity": "ADCPNorthVelocity",
        "Heading": "ADCPHeading"
         }
    }
}
