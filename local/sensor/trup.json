// Derived sensor for true winds, port side
{
    // data_id
    "trup": {
        "name": "trup",
        "model": "TrueWinds",    // references a definition in sensor_model/
        "fields": {               // references definitions in message/
            "TrueWindDir": "PortTrueWindDir",
            "TrueWindSpeed": "PortTrueWindSpeed",
            "ApparentWindDir": "PortApparentWindDir"
         }
    }
}
